name: Deploy Nginx Configuration

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Nginx (for syntax validation)
        run: |
          sudo apt-get update
          sudo apt-get install -y nginx

      - name: Run basic validation
        run: |
          chmod +x scripts/test.sh
          ./scripts/test.sh

      - name: Validate Nginx syntax (dry-run)
        run: |
          # Create temporary nginx.conf for testing
          sudo mkdir -p /tmp/nginx-test/sites
          sudo mkdir -p /tmp/nginx-test/snippets
          sudo cp nginx/sites/* /tmp/nginx-test/sites/
          sudo cp nginx/snippets/* /tmp/nginx-test/snippets/
          
          # Test each site config
          for conf in nginx/sites/*.conf; do
            echo "Testing $(basename $conf)..."
            # Note: Full test requires actual paths, this is a basic check
            sudo nginx -t -c /etc/nginx/nginx.conf 2>&1 | head -1
          done

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: validate
    environment: snoroc-nginx
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy configuration files
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ vars.SERVER_HOST }}
          username: ${{ vars.SERVER_USER }}
          key: ${{ vars.SERVER_SSH_KEY }}
          port: ${{ vars.SERVER_PORT }}
          source: "nginx/sites/*,nginx/snippets/*,scripts/deploy.sh"
          target: ${{ vars.DEPLOY_TEMP_DIR }}
          strip_components: 0

      - name: Execute deployment script
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ vars.SERVER_HOST }}
          username: ${{ vars.SERVER_USER }}
          key: ${{ vars.SERVER_SSH_KEY }}
          port: ${{ vars.SERVER_PORT }}
          script: |
            cd ${{ vars.DEPLOY_TEMP_DIR }}
            chmod +x scripts/deploy.sh
            sudo ./scripts/deploy.sh
            
            # Cleanup
            cd /
            rm -rf ${{ vars.DEPLOY_TEMP_DIR }}

      - name: Verify deployment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ vars.SERVER_HOST }}
          username: ${{ vars.SERVER_USER }}
          key: ${{ vars.SERVER_SSH_KEY }}
          port: ${{ vars.SERVER_PORT }}
          script: |
            echo "Checking Nginx status..."
            sudo systemctl status nginx --no-pager
            echo ""
            echo "Testing configuration..."
            sudo nginx -t

  health-check:
    name: Health Check
    runs-on: ubuntu-latest
    needs: deploy
    environment: snoroc-nginx
    
    steps:
      - name: Wait for stabilization
        run: |
          echo "⏳ Attente de 5 secondes pour stabilisation..."
          sleep 5

      - name: Check dev site
        run: |
          SITE="${{ vars.SITE_URL_DEV }}"
          echo "🔍 Vérification de https://$SITE..."
          
          STATUS_CODE=$(curl -o /dev/null -s -w "%{http_code}" "https://$SITE")
          if [ "$STATUS_CODE" != "200" ]; then
            echo "❌ $SITE répond avec HTTP $STATUS_CODE"
            exit 1
          fi
          echo "✅ $SITE - HTTP 200 OK"
          
          CONTENT=$(curl -s "https://$SITE")
          if echo "$CONTENT" | grep -qiE "nginx error|Bad Gateway|Welcome to nginx|403 Forbidden|404 Not Found"; then
            echo "❌ $SITE renvoie une page d'erreur"
            exit 1
          fi
          echo "✅ $SITE - Contenu valide"

      - name: Check prod site
        run: |
          SITE="${{ vars.SITE_URL }}"
          echo "🔍 Vérification de https://$SITE..."
          
          STATUS_CODE=$(curl -o /dev/null -s -w "%{http_code}" "https://$SITE")
          if [ "$STATUS_CODE" != "200" ]; then
            echo "❌ $SITE répond avec HTTP $STATUS_CODE"
            exit 1
          fi
          echo "✅ $SITE - HTTP 200 OK"
          
          CONTENT=$(curl -s "https://$SITE")
          if echo "$CONTENT" | grep -qiE "nginx error|Bad Gateway|Welcome to nginx|403 Forbidden|404 Not Found"; then
            echo "❌ $SITE renvoie une page d'erreur"
            exit 1
          fi
          echo "✅ $SITE - Contenu valide"

      - name: Deployment success
        run: |
          echo "🎉 Déploiement validé avec succès !"
          echo "✅ DEV: https://${{ vars.SITE_URL_DEV }}"
          echo "✅ PROD: https://${{ vars.SITE_URL }}"

