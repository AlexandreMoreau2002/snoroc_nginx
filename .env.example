# ============================================
# GITHUB VARIABLES (À CONFIGURER)
# ============================================
# Ces variables doivent être configurées dans :
# Settings → Secrets and variables → Actions → Variables
# de votre repository GitHub

# IP ou hostname de votre serveur OVH
SERVER_HOST=51.178.x.x

# Utilisateur SSH (généralement 'ubuntu' sur Ubuntu)
SERVER_USER=ubuntu

# Port SSH
SERVER_PORT=22

# Répertoire temporaire pour le déploiement
DEPLOY_TEMP_DIR=/tmp/nginx-deploy


# ============================================
# GITHUB SECRETS (À CONFIGURER)
# ============================================
# Ces variables doivent être configurées dans :
# Settings → Secrets and variables → Actions → Secrets
# de votre repository GitHub

# Clé privée SSH pour la connexion (générer avec ssh-keygen)
# Commande : cat ~/.ssh/snoroc_deploy
SERVER_SSH_KEY=-----BEGIN OPENSSH PRIVATE KEY-----
...
-----END OPENSSH PRIVATE KEY-----
